daemon off;
pid ./nginx.pid;

events {
  worker_connections 1024;
}

http {
  server {
    listen 10001;
    access_log off;
    error_log /dev/null;
    location / {
      return 200 'OK';
    }
  }
}

